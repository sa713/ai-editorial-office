# Writer Notes

Task ID: `TASK-0003`

Owner: `writer_agent`

Stage: `writing`

## decisions

- Used `несколько молодых специалистов`, not an exact count, because the current task did not specify a number.
- Kept `УЭК` without explanation because the audience is internal.
- Avoided `мы рады сообщить`, `уникальная возможность`, praise-heavy framing, and motivational language.
- Avoided `команда` because the user explicitly flagged it as an unwanted corporate pattern.
- Did not include a link placeholder because the user requested announcement text, not a send-ready email template with operational blocks.

## variant strategy

- `restrained`: maximum clarity and low risk.
- `warmer`: more human closeness while staying short.
- `more editorial/journal-like`: strongest magazine-like framing around the new multi-voice format.

## known risks for review

- `restrained` may be too plain.
- `warmer` may still carry a mild HR-adjacent phrase around `молодых специалистов`.
- `more editorial/journal-like` may need one bounded softening to avoid a promotional close.
