# Agents and pipelines check

## AGENTS.md

Visual freeze does not conflict with core roles.

Artist Agent remains a legalized extension role, but inactive by default.

Entry discipline is preserved: TASK-folder, editorial project, and existing editorial workflow tasks still require editorial routing before production.

Direct-production bypass for TASK/editorial tasks remains forbidden unless the user explicitly asks to bypass the editorial process.

Ordinary text tasks do not require visual artifacts.

## Pipeline files

`article_pipeline.md`: OK. Core roles are default. Extension roles are allowed only under `AGENTS.md` conditions. No required visual artifacts.

`research_pipeline.md`: OK. Research remains evidence-only and does not require Artist Agent or visual artifacts.

`review_pipeline.md`: OK. Review remains a gate for text/material validation. It mentions `artist-agent` only as a possible slug when visual-extension conditions apply.

`social_pipeline.md`: OK. Core roles are default. No required visual artifacts.

`ux_writing_pipeline.md`: OK. Core roles are default. No required visual artifacts.

## Role admissibility

Role admissibility remains correct across pipelines: extension roles may be used only when explicitly legalized by `AGENTS.md` and only within their bounded scope.

The frozen visual subsystem does not block normal text pipeline execution.

