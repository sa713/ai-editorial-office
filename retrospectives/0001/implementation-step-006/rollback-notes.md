# Rollback notes

## Full rollback

To fully rollback Step 6:

1. Remove `custom workflow mini-contract` from `orchestration_plan_template.md`.
2. Restore the previous no-pipeline-fits sentence in `chief_editor.md`.
3. Remove source material as data and instruction promotion paragraphs from `AGENTS.md`.
4. Remove source-as-data responsibility, forbidden action, and source authority note from `research_agent.md`.
5. Remove Step 6 normalization notes from `project-state.md`.

## Partial rollback

If custom workflow mini-contract becomes too common:

- keep the Chief Editor note;
- remove the template section and require a one-line explanation in existing orchestration fields.

If source trust language causes too much source labeling:

- keep only the AGENTS rule;
- remove additional Research Agent elaboration.

If instruction promotion needs tighter control:

- defer to a later governance decision rather than adding approval chains here.

## Rollback safety

Rollback does not require changing lifecycle, statuses, compact path, review model, governance state, agent set, or existing task folders.
